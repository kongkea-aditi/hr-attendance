<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="hr_employee_view_form_ip_check" model="ir.ui.view">
        <field name="name">hr.employee.view.form.ip.check</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//page[@name='hr_settings']//group[@name='identification_group']"
                position="inside"
            >
                <field
                    name="bypass_ip_check"
                    widget="boolean_toggle"
                    attrs="{'invisible': [('work_location_id', '=', False)]}"
                    help="When enabled, this employee can check in/out from any IP address regardless of work location settings"
                />
                <field name="work_location_id" />
            </xpath>
        </field>
    </record>
</odoo>
