<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Extend work location form -->
    <record id="hr_work_location_view_form_inherit" model="ir.ui.view">
        <field name="name">hr.work.location.view.form.inherit.ip.check</field>
        <field name="model">hr.work.location</field>
        <field name="inherit_id" ref="hr.hr_work_location_form_view" />
        <field name="arch" type="xml">
            <xpath expr="//sheet/group" position="after">
                <group
                    string="IP Attendance Check"
                    name="ip_check"
                    groups="hr.group_hr_manager"
                >
                    <field name="check_ip" />
                    <field
                        name="allowed_cidr_ids"
                        attrs="{'invisible': [('check_ip', '=', False)]}"
                        context="{'default_work_location_id': active_id}"
                    >
                        <tree editable="bottom">
                            <field name="sequence" widget="handle" />
                            <field name="name" placeholder="e.g. Office Network" />
                            <field name="cidr" placeholder="e.g. 192.168.1.0/24" />
                            <field name="active" />
                        </tree>
                    </field>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
